// @ts-check
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';

// loomweave.foundryside.dev — the Loomweave member spoke (IA §1.3, §1.4).
//   site:  https://loomweave.foundryside.dev
//   base:  '/'   (each member site is a domain root — no subpath gymnastics)
// Cross-subdomain links to siblings are ABSOLUTE https://{member}.foundryside.dev
// URLs generated by the shared @weft/site-kit data, so this site cannot drift
// from the hub or its peers.
export default defineConfig({
  site: 'https://loomweave.foundryside.dev',
  base: '/',
  integrations: [react()],
});
